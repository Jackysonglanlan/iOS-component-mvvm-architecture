platform :ios, '9.0'

target 'Component' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

pod 'A', :path => './'
pod 'Network', :path => './'
pod 'module-mvvm', :path => './'

end
